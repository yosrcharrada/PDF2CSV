"""The local web application.

A thin caller of :func:`pdf2csv.run`, exactly like the CLI and the notebook.
Nothing in here knows how a PDF is parsed, and nothing in ``core`` knows this
package exists.
"""
